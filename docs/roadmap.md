# 앞으로 할 일

2026-09-10 갱신. 새 Figma 시안 반영 작업의 남은 작업 목록입니다.
결정 근거는 [decisions.md](decisions.md), 실측 토큰은 [design-tokens.md](design-tokens.md),
클라이언트 의견은 [client-feedback.md](client-feedback.md),
모바일 변환 현황은 [mobile-ui.md](mobile-ui.md),
새 시안·요구사항 명세서 확인 결과는 [figma-spec-review.md](figma-spec-review.md)를 참고하세요.

> **구현 기준 시안**: Figma `나지르`(`0OWqWD2aHB1gzSNnshAlei`) → **`개발자용 페이지`** →
> 섹션 `홈페이지 디자인`(프레임 13개). 랜딩 노드 `355:945`.
> 레이아웃 수치는 `design-tokens.md`와 동일하고, 에셋 교체와 지적사항 반영만 진행된 상태입니다.

---

## 완료된 것

- [x] 시안 전체(프레임 13개 / 레이어 1,735개) 토큰 추출 → `design-tokens.md`
- [x] 클라이언트 코멘트 26개 정리 → `client-feedback.md`
- [x] 폰트 확보·배치 (`public/fonts/`) 및 렌더링 검증
- [x] `@font-face` 3종 (`app/globals.css`)
- [x] Tailwind에 `ds-*` 색상 6종 / `font-heir` / `font-griun` / `max-w-content` 추가
- [x] `/fonts/*` 영구 캐시 헤더 (`next.config.ts`)
- [x] `.gitattributes` — 폰트 바이너리 손상 방지 (`core.autocrlf=true` 환경)
- [x] **모바일 랜딩페이지 시안** (390 폭) — Figma 섹션 `모바일 UI` → [mobile-ui.md](mobile-ui.md)

- [x] **1단계 공통 셸** (GNB · 푸터)
- [x] **2단계 페이지 5개** — 랜딩 · 대하여 4 · 제작 과정 2 · 함께하는 사람들 3탭 · 후원과 기도 2
- [x] 히어로·카드 아이콘 이미지 반영 (`public/images/`)
- [x] 편지 이미지 어드민 관리 (`0008`)

**남은 것은 3단계 응원 게시판과 4·5단계 정리·모바일입니다.**

---

## 지금 순서 — 웹(데스크톱)부터

모바일은 Figma 랜딩 1장까지만 해두고 **뒤로 미룹니다.** 웹 레이아웃이 굳기 전에 모바일을 그리면
두 번 그리게 됩니다.

1. **1단계 공통 셸** — `Header.tsx` / `Footer.tsx` 교체. 13개 화면 전부에 들어가는 부분이라
   여기가 끝나야 2단계가 빨라집니다.
2. **2단계 페이지별 시안 반영** — 라우트 5개. `함께하는 사람들`만 신설이고 나머지는 기존 파일 수정입니다.
3. **3단계 응원 게시판** — 신규 기능. **지금 유일하게 답변 대기로 막혀 있는 단계입니다.**

**1·2단계를 막는 항목은 하나도 없습니다.** 임시 에셋(로고 텍스트 · AI 생성 아이콘 · 히어로 임시본)으로
그대로 진행하고, 최종본이 오면 교체만 하면 됩니다.

---

## 1단계 — 공통 셸

시안 13개 프레임에 동일하게 들어가는 부분이라 먼저 만들면 나머지가 빨라집니다.
대상 파일: `components/Header.tsx` · `components/Footer.tsx` · `app/(site)/layout.tsx`

- [x] **GNB 교체** — 영문 3개(`The Work`/`In the Making`/`Join Us`) →
      한글 5개 + `후원 바로가기` CTA(구글폼 직결)
  - 높이 `80px`, 메뉴 그룹 오토레이아웃 `gap: 48px`, **완전 중앙 정렬**(시안 오차 무시)
  - CTA `144×37`, 배경 `ds-key2`, 라벨 `후원 바로가기`
  - 활성 메뉴 = **노랑 + 밑줄** — 새 시안에서 확정, #26 보류 해제
  - GNB 라벨은 `나지르에 대하여`(꺾쇠 없음), 페이지 제목은 `<나지르>에 대하여` — 표기 통일 여부 확인 필요
- [x] **푸터 교체** — 3컬럼(`문의` / `더 알아보기` / `후원하기`), 균등 3분할,
      간격 `64px`, 구분선 `0.5px solid #FFFFFF`
  - 연락처는 **마스킹 없이 시안 그대로 노출** — 대표 확인 완료 (코멘트 #47)
  - 로고는 임시 텍스트 → 최종 로고 나오면 교체 (코멘트 #24)
- [x] 콘텐츠 컨테이너 `max-w-content`(1398px) 적용

## 2단계 — 기존 페이지 시안 반영

각 페이지를 만들 때 `ds-*` 토큰을 씁니다. 옛 토큰은 마지막에 제거합니다.

| 시안 화면 | 라우트 | 상태 |
|---|---|---|
| 랜딩페이지 | `/` | ✅ 완료 |
| 〈나지르〉에 대하여 4개 | `/about/[slug]` | ✅ 완료 |
| 제작 과정 2개 | `/process/[slug]` | ✅ 완료 |
| 함께하는 사람들 3탭 | `/people?tab=` | ✅ 완료 |
| 후원과 기도 2개 | `/join/[slug]` | ✅ 완료 |
| 응원 게시판 | `/guestbook` | 신설 (3단계) — **아직 404** |

상위 메뉴는 전부 `/[상위]/[slug]` 구조이고, 상위 경로는 첫 장으로 리다이렉트합니다.
하단 도트는 `components/SectionDots.tsx` 하나를 공유합니다.

- [x] **랜딩페이지** — 히어로(`1920×757`, 오버레이는 이미지에 구워져 있음) + 카드 5개
      (`gap: 12px`, 각 `270×350`, 반경 8px, 그림자)
  - 아이콘 원본이 `181×160`~`139×122`로 제각각 → 통일 박스 + `object-fit: contain`으로 흡수 (#3)
- [x] **\<나지르\>에 대하여** — 4개 서브페이지 `/about/[slug]`
      (01 `greeting` / 02 `praysound` / 03 `work` / 04 `characters`), `/about`은 01로 리다이렉트
  - 순서: 작품 개요 → 로그라인·시놉시스 → 주요 등장인물 (코멘트 #29) ✅
  - 편지 캐러셀 (01·02에만) — 어드민 관리형. 장수는 등록한 개수만큼 `1 / N`이고
    시안의 `1 / 5`는 고정값이 아니다(디자이너 확인 완료). `0008` 마이그레이션 필요
  - 페이지네이션 도트 **4개** — 시안 수정 완료 (코멘트 #45) ✅
  - 좌측 본문은 01·02만 채웠다. 03·04는 시안이 더미라 비워뒀다(우측과 중복 방지)
- [x] **제작 과정** — `/process/[slug]` 2개 (01 `schedule` / 02 `budget`)
  - 01 카드 2장 — `지나온 이야기`(완료) 어두운 카드 / `앞으로 걸어갈 이야기`(진행 중·예정) 노란 카드
  - 기본 5개 + `외 N개의 기록 더보기` (시안은 둘 다 "외 4개"로 복붙돼 있어 실제 남은 개수로 계산)
  - 02 총액 + 항목 8개 2열, 금액 비공개 (코멘트 #31)
  - ⚠️ 시안 총액 표기는 `9,000,000원`, 현재 데이터는 `₩ 9,000,000` — 관리자에서 맞출 것
- [x] **함께하는 사람들** — 최상위 라우트 신설, 탭 3개(헤더진/스탭진/배우)
  - 그리드 `1398×708`, 행 `gap: 8px` / 열 `gap: 12px`, 카드 `270×350`
  - 카드 = 사진 + 역할 + 이름, 클릭 시 `/people/[id]` (코멘트 #37)
  - ⚠️ 시안 카드는 아직 빈 노란 박스이고, 요구사항 명세서(32~34행)에는 상세페이지 항목이 없습니다.
    **코멘트 #46 답변 전까지 카드 링크 구조는 확정 금지** — [figma-spec-review.md](figma-spec-review.md) 6-3 참고
  - 사진은 기존 `PhotoField` + Supabase Storage 재사용
- [x] **후원과 기도** — `/join/[slug]` 2개 (01 `support` / 02 `prayer`), `/join`은 01로 리다이렉트
  - 01 후원 안내(구글폼 버튼 + 점선 계좌 박스 + 복사 버튼) · 02 기도 제목 6개 ✅
  - ⚠️ **기존 Q&A 블록(joey.team 외부 링크)을 제거했다.** 시안에 없고 응원 게시판(3단계)이
    대신할 자리다. 3단계 전까지 방문자가 메시지를 남길 경로가 없다 — 유지가 필요하면 되살릴 것

## 3단계 — 응원 게시판 (신규)

설계 트레이드오프는 `decisions.md` "응원 게시판" 절에 정리돼 있습니다.

**1차(읽기·쓰기)는 완료.** 하트·대댓글은 확정 대기라 빠져 있습니다.

- [x] 테이블 마이그레이션 `0009_guestbook.sql`
  - `guestbook_entries` — 이름, 메시지, 생성일, `ip_hash`, `is_held`(보류 플래그)
  - `is_hearted` · `guestbook_replies`는 확정 후 별도 마이그레이션
  - 공개 읽기는 RLS로 `is_held = false`만. anon 직접 insert는 막고
    `submit_guestbook_entry` SECURITY DEFINER 함수로만 받는다
    (브라우저에 노출된 anon 키로 우회 삽입할 수 없게 — `record_visit`과 같은 패턴)
- [x] 공개 작성 API `POST /api/guestbook` + 봇 대응 4종
  - Rate limit — IP 해시, **시간당 15건** (단체 관람 오탐 여유)
  - Honeypot 필드 (`autocomplete="off"`, 화면 밖 배치)
  - 제출 시간 검사 (3초 미만 차단)
  - 링크 포함 글 보류 (삭제 아님)
  - 허니팟·시간 위반은 이유를 알려주지 않는다(우회 힌트가 되므로)
- [x] 쪽지 카드 렌더
  - 악보 배경 정사각형(`note-square`)·직사각형(`note-wide`) 교대
  - 테이프 `77×36`, 각도 **8° / −8° 교대**
  - 배치: 좌상단 이름 / 가운데 내용 / 우하단 날짜
  - ⚠️ 좌하단 댓글 아이콘은 대댓글이 없어 넣지 않았다(눌러도 안 되는 아이콘 방지)
- [x] **페이지네이션** — `?page=N`, 한 페이지 24개
  - 시안·명세에 게시판 페이지네이션 UI가 없어 편지 캐러셀의 `‹ 1 / N ›` 표기를 가져왔다
  - 링크라서 공유·뒤로가기가 되고, 글을 남긴 뒤 서버가 다시 그려도 어긋날 상태가 없다
  - 범위 밖·잘못된 `page` 값은 404 대신 1페이지로 떨어진다
  - 글을 남기면 1페이지로 보낸다(새 글이 항상 맨 앞에 붙으므로)
- [ ] 대댓글 확장 인터랙션 — 아이콘 클릭 시 레이어 확장
  - **디자이너 요청 필요**: 9-slice용 3분할 악보 이미지 **또는** 답글용 작은 쪽지 이미지
  - CSS 세로 늘림은 악보 오선이 왜곡되므로 사용하지 않음
- [ ] 어드민 CRUD — 삭제, 답글 작성, 하트 토글, 보류 글 검토

## 4단계 — 정리

- [x] **옛 팔레트 제거** — `gold` / `gold-soft` / `gold-deep` / `paper` / `velvet` / `velvet-2` /
      `ink` / `stage` 를 전부 `ds-*` 로 옮기고 `tailwind.config.ts` 에서 삭제했습니다(203곳).
      공개 화면뿐 아니라 관리자 화면도 같은 토큰을 씁니다.
- [x] **Google Fonts `<link>` 제거** — 공개 화면은 자체 호스팅하는 시안 폰트만 쓰고,
      관리자 화면과 기본 본문은 시스템 폰트로 갑니다. 외부 폰트 요청 0건.
- [x] **미사용 컴포넌트 정리** — `HeroBackdrop` · `Spotlight` · `Accordion` · `StatusChip`.
      랜딩·제작 과정을 새 시안으로 갈아엎으면서 참조가 사라졌습니다.
- [ ] 본문 폰트가 확정되면(코멘트 #38) 해당 폰트만 `<link rel="preload">` 추가

## 5단계 — 모바일 (웹 확정 후)

웹 레이아웃이 굳기 전에는 손대지 않습니다. Figma 모바일 시안은 **랜딩 1장만** 있고 나머지 11개는
미착수입니다. 좌표·변환 규칙·주의사항은 [mobile-ui.md](mobile-ui.md)에 있습니다.

- [ ] Figma 모바일 시안 나머지 11개 — **Figma 데스크톱 앱에서 직접** (MCP는 시안 폰트를 못 씀)
- [ ] 모바일 브레이크포인트 구현 — GNB 햄버거 / 카드 가로형 1열 / 푸터 1컬럼 / 그리드 2열

---

## 답변 대기 중인 것

| 항목 | 누구에게 | 영향 범위 | 웹 1·2단계 차단? |
|---|---|---|---|
| **"우리 하트"가 운영진 하트인지 방문자 좋아요인지** | 클라이언트 | 응원 게시판 스키마 | ❌ 3단계만 |
| **대댓글 확장용 이미지 에셋** (3분할 또는 답글 쪽지) | 홍빛 | 응원 게시판 | ❌ 3단계만 |
| **함께하는 사람들 카드 클릭 → 상세페이지 여부** (코멘트 #46, 답글 없음) | 클라이언트 | `/people/[id]` 존치 | ⚠️ 2단계 일부 |
| **본문 폰트** ① 전부 빛의계승자체 ② 제목만 + 본문은 가독성 폰트 | eunmin / 홍빛 (코멘트 #38 답글 확인) | 전 페이지 | 아니오 — 제목은 확정, 본문만 나중에 교체 |
| **랜딩 카드 아이콘 크기 통일** (이미지는 `icon1~5.png`로 교체됨, 크기는 제각각 그대로) | 홍빛 | 랜딩페이지 | 아니오 — `object-fit`으로 흡수 |
| **로고 이미지** (푸터가 텍스트 임시) | 홍빛 | 푸터 | 아니오 — 텍스트로 진행 |
| **히어로 이미지 최종본 여부** (`1.png`로 교체됨) | 홍빛 | 랜딩페이지 | 아니오 |

**해소된 항목**

- ~~GNB 활성 밑줄 간격~~ → 새 시안에서 노랑 + 밑줄로 확정 (#26)
- ~~푸터 전화번호 공개 여부~~ → **마스킹 없이 공개로 확정** (#47, 대표 확인 완료)
- ~~대하여 페이지네이션 도트 개수~~ → 4개로 시안 수정 완료 (#45)

**실제로 막힌 것은 3단계 응원 게시판과 `함께하는 사람들` 카드 링크 구조뿐입니다.**
위 3개(하트 의미 · 대댓글 에셋 · #46)를 먼저 물어두면, 웹 1·2단계를 진행하는 동안 답이 옵니다.

---

## 배포 전 점검

- [ ] **마이그레이션 적용** — `0007`(people_groups 라벨 팀원→스탭진) · ~~`0008`(about_letters)~~ 적용 완료 ·
      `0009`(guestbook). Supabase 대시보드 SQL Editor에서 직접 실행해야 합니다
- [ ] **관리자에서 고칠 값** — 은행명 `KAKAOBANK` → `카카오뱅크`,
      예산 총액 `₩ 9,000,000` → 시안 표기 `9,000,000원`
- [ ] Vercel 배포 후 `/fonts/HeirOfLight-Regular.otf` 200 확인
- [ ] 응답 헤더 `Cache-Control: public, max-age=31536000, immutable` 확인
- [ ] Vercel Hobby는 약관상 **개인·비상업 프로젝트 전용**.
      후원 계좌를 안내하는 사이트라 완전히 무관하다고 보기는 어려움 — 인지 필요
- [ ] 폰트 전송량: 신규 방문 1명당 최대 3.81MB
      (woff2 2.68MB + OTF gzip 573KB·586KB). Hobby 100GB/월 기준 약 27,000 신규 방문분
- [ ] `public/fonts/`에 woff2가 없어 매 방문 OTF를 받습니다. woff2를 넣으면 전송량이 크게 줍니다

---

## 다른 기기에서 이어서 작업할 때

```bash
git pull
npm install
npm run dev
```

폰트 파일은 저장소에 포함돼 있어 별도 내려받기가 필요 없습니다.
`.claude/`는 gitignore 대상이라 `launch.json`은 따라오지 않습니다.

# 앞으로 할 일

2026-09-02 기준. 새 Figma 시안 반영 작업의 남은 작업 목록입니다.
결정 근거는 [decisions.md](decisions.md), 실측 토큰은 [design-tokens.md](design-tokens.md),
클라이언트 의견은 [client-feedback.md](client-feedback.md),
모바일 변환 현황은 [mobile-ui.md](mobile-ui.md)를 참고하세요.

---

## 완료된 것

- [x] 시안 전체(프레임 13개 / 레이어 1,735개) 토큰 추출 → `design-tokens.md`
- [x] 클라이언트 코멘트 26개 정리 → `client-feedback.md`
- [x] 폰트 확보·배치 (`public/fonts/`) 및 렌더링 검증
- [x] `@font-face` 3종 (`app/globals.css`)
- [x] Tailwind에 `ds-*` 색상 6종 / `font-heir` / `font-griun` / `max-w-content` 추가
- [x] `/fonts/*` 영구 캐시 헤더 (`next.config.ts`)
- [x] `.gitattributes` — 폰트 바이너리 손상 방지 (`core.autocrlf=true` 환경)
- [x] **모바일 랜딩페이지 시안** (390 폭) — Figma 섹션 `모바일 UI` → [mobile-ui.md](mobile-ui.md)

---

## 1단계 — 공통 셸

시안 13개 프레임에 동일하게 들어가는 부분이라 먼저 만들면 나머지가 빨라집니다.

- [ ] **GNB 교체** — 현재 영문 3개(`The Work`/`In the Making`/`Join Us`) →
      한글 5개 + `후원 바로가기` CTA
  - 높이 `80px`, 메뉴 그룹 오토레이아웃 `gap: 48px`, **완전 중앙 정렬**(시안 오차 무시)
  - CTA `144×37`, 배경 `ds-key2`, 라벨 `후원 바로가기`
  - 활성 메뉴 밑줄 — 간격 미정 (코멘트 #26)
- [ ] **푸터 교체** — 3컬럼(`문의` / `더 알아보기` / `후원하기`), 컬럼 폭 `413px`,
      간격 `64px`, 구분선 `0.5px solid #FFFFFF`, `left: 0`
  - 전화번호 `XXX-XXXX-XXXX` 마스킹
  - 로고는 임시 텍스트 → 최종 로고 나오면 교체 (코멘트 #24)
- [ ] 콘텐츠 컨테이너 `max-w-content`(1398px) 적용

## 2단계 — 기존 페이지 시안 반영

각 페이지를 만들 때 `ds-*` 토큰을 씁니다. 옛 토큰은 마지막에 제거합니다.

- [ ] **랜딩페이지** — 히어로(`1920×757`, 오버레이 `rgba(0,0,0,.2)`) + 카드 5개
      (`gap: 12px`, 각 `270×350`)
- [ ] **\<나지르\>에 대하여** — 4개 서브페이지
      (01 연출의 인사말 / 02 PRAY SOUND / 03 작품 소개 / 04 등장인물)
  - 순서: 작품 개요 → 로그라인·시놉시스 → 주요 등장인물 (코멘트 #29)
  - 편지 `1 / 5` 캐러셀 (01·02에만)
  - 페이지네이션 도트 처리 **보류** (decisions.md 10번)
- [ ] **제작 과정** — 01 타임라인(지나온/앞으로, `더보기` 토글) · 02 예산(항목만, 금액 비공개)
- [ ] **함께하는 사람들** — 최상위 라우트 신설, 탭 3개(헤더진/스탭진/배우)
  - 그리드 `1398×708`, 행 `gap: 8px` / 열 `gap: 12px`, 카드 `270×350`
  - 카드 = 사진 + 역할 + 이름, 클릭 시 `/people/[id]` (코멘트 #37)
  - 사진은 기존 `PhotoField` + Supabase Storage 재사용
- [ ] **후원과 기도** — 01 후원 안내(구글폼 버튼 + 계좌번호 복사) · 02 기도 제목 6개

## 3단계 — 응원 게시판 (신규)

설계 트레이드오프는 `decisions.md` "응원 게시판" 절에 정리돼 있습니다.

- [ ] 테이블 마이그레이션
  - `guestbook_entries` — 이름, 메시지, 생성일, `ip_hash`, `is_hearted`(운영진 하트), 보류 플래그
  - `guestbook_replies` — 1:N (운영진 답글)
- [ ] 공개 작성 API + 봇 대응 4종
  - Rate limit (IP 해시, **시간당 10건 이상으로 넉넉히** — 단체 관람 오탐 주의)
  - Honeypot 필드 (`autocomplete="off"`)
  - 제출 시간 검사 (3초 미만 차단)
  - 링크 포함 글 보류 (삭제 아님)
- [ ] 쪽지 카드 렌더
  - `428×224`, 악보 배경 정사각형·직사각형 교대
  - 테이프 `77×36`, 각도 **8° / −8° 교대**
  - 배치: 좌상단 이름 / 가운데 내용 / 좌하단 아이콘 / 우하단 날짜
- [ ] 대댓글 확장 인터랙션 — 아이콘 클릭 시 레이어 확장
  - **디자이너 요청 필요**: 9-slice용 3분할 악보 이미지 **또는** 답글용 작은 쪽지 이미지
  - CSS 세로 늘림은 악보 오선이 왜곡되므로 사용하지 않음
- [ ] 어드민 CRUD — 삭제, 답글 작성, 하트 토글, 보류 글 검토

## 4단계 — 정리

- [ ] 옛 팔레트 제거 (`gold` / `gold-soft` / `gold-deep` / `paper` / `velvet` / `velvet-2` / `ink`)
- [ ] Google Fonts `<link>` 제거 (`app/layout.tsx`) — 시안 폰트 전환 완료 후
- [ ] 본문 폰트가 확정되면 해당 폰트만 `<link rel="preload">` 추가
- [ ] 모바일 브레이크포인트 정리

---

## 막혀 있는 것 — 답이 와야 진행 가능

| 항목 | 누구에게 | 영향 범위 |
|---|---|---|
| **본문 폰트** ① 전부 빛의계승자체 ② 제목만 + 본문은 가독성 폰트 | eunmin / 홍빛 (코멘트 #38 답글 확인) | 전 페이지 |
| **"우리 하트"가 운영진 하트인지 방문자 좋아요인지** | 클라이언트 | 응원 게시판 스키마 |
| **대댓글 확장용 이미지 에셋** (3분할 또는 답글 쪽지) | 홍빛 | 응원 게시판 |
| **랜딩 카드 아이콘 5종 최종본** (현재 AI 생성 임시본, 크기 제각각) | 홍빛 | 랜딩페이지 |
| **로고 이미지** (현재 텍스트 임시) | 홍빛 | 푸터·GNB |
| **히어로 이미지 최종본** | 홍빛 | 랜딩페이지 |
| **GNB 활성 밑줄 간격** | 홍빛 (코멘트 #26) | 공통 셸 |
| **푸터 전화번호 공개 여부** | 클라이언트 | 푸터 |

---

## 배포 전 점검

- [ ] Vercel 배포 후 `/fonts/HeirOfLight-Regular.otf` 200 확인
- [ ] 응답 헤더 `Cache-Control: public, max-age=31536000, immutable` 확인
- [ ] Vercel Hobby는 약관상 **개인·비상업 프로젝트 전용**.
      후원 계좌를 안내하는 사이트라 완전히 무관하다고 보기는 어려움 — 인지 필요
- [ ] 폰트 전송량: 신규 방문 1명당 최대 3.81MB
      (woff2 2.68MB + OTF gzip 573KB·586KB). Hobby 100GB/월 기준 약 27,000 신규 방문분

---

## 다른 기기에서 이어서 작업할 때

```bash
git pull
npm install
npm run dev
```

폰트 파일은 저장소에 포함돼 있어 별도 내려받기가 필요 없습니다.
`.claude/`는 gitignore 대상이라 `launch.json`은 따라오지 않습니다.

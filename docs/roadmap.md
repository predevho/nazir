# 앞으로 할 일

2026-09-11 갱신. 새 Figma 시안 반영 작업의 남은 작업 목록입니다.
결정 근거는 [decisions.md](decisions.md), 실측 토큰은 [design-tokens.md](design-tokens.md),
클라이언트 의견은 [client-feedback.md](client-feedback.md),
모바일 변환 현황은 [mobile-ui.md](mobile-ui.md),
새 시안·요구사항 명세서 확인 결과는 [figma-spec-review.md](figma-spec-review.md)를 참고하세요.

> **구현 기준 시안**: Figma `나지르`(`0OWqWD2aHB1gzSNnshAlei`) → **`개발자용 페이지`** →
> 섹션 `홈페이지 디자인`(프레임 13개). 랜딩 노드 `355:945`.
> 레이아웃 수치는 `design-tokens.md`와 동일하고, 에셋 교체와 지적사항 반영만 진행된 상태입니다.

---

## 완료된 것

- [x] 시안 전체(프레임 13개 / 레이어 1,735개) 토큰 추출 → `design-tokens.md`
- [x] 클라이언트 코멘트 26개 정리 → `client-feedback.md`
- [x] 폰트 확보·배치 (`public/fonts/`) 및 렌더링 검증
- [x] `@font-face` 3종 (`app/globals.css`)
- [x] Tailwind에 `ds-*` 색상 6종 / `font-heir` / `font-griun` / `max-w-content` 추가
- [x] `/fonts/*` 영구 캐시 헤더 (`next.config.ts`)
- [x] `.gitattributes` — 폰트 바이너리 손상 방지 (`core.autocrlf=true` 환경)
- [x] **모바일 랜딩페이지 시안** (390 폭) — Figma 섹션 `모바일 UI` → [mobile-ui.md](mobile-ui.md)

- [x] **1단계 공통 셸** (GNB · 푸터)
- [x] **2단계 페이지 5개** — 랜딩 · 대하여 4 · 제작 과정 2 · 함께하는 사람들 3탭 · 후원과 기도 2
- [x] 히어로·카드 아이콘 이미지 반영 (`public/images/`)
- [x] 편지 이미지 어드민 관리 (`0008`)

- [x] **3단계 응원 게시판** — 읽기·쓰기·답글·규칙 선별·제작팀 하트
- [x] **6단계 운영 보강** — 아래 참조

**남은 것은 운영진이 채울 데이터(인물 43명 사진·한 줄 소개·약력, 사진은 9월 중 전달 예정)뿐입니다.**

---

## 지금 순서 — 웹(데스크톱)부터

모바일은 Figma 랜딩 1장까지만 해두고 **뒤로 미룹니다.** 웹 레이아웃이 굳기 전에 모바일을 그리면
두 번 그리게 됩니다.

1. **1단계 공통 셸** — `Header.tsx` / `Footer.tsx` 교체. 13개 화면 전부에 들어가는 부분이라
   여기가 끝나야 2단계가 빨라집니다.
2. **2단계 페이지별 시안 반영** — 라우트 5개. `함께하는 사람들`만 신설이고 나머지는 기존 파일 수정입니다.
3. **3단계 응원 게시판** — 신규 기능.

**1·2·3단계 모두 끝났습니다.** 임시 에셋(AI 생성 아이콘 · 히어로 임시본)은 그대로 두고,
최종본이 오면 교체만 하면 됩니다.

---

## 1단계 — 공통 셸

시안 13개 프레임에 동일하게 들어가는 부분이라 먼저 만들면 나머지가 빨라집니다.
대상 파일: `components/Header.tsx` · `components/Footer.tsx` · `app/(site)/layout.tsx`

- [x] **GNB 교체** — 영문 3개(`The Work`/`In the Making`/`Join Us`) →
      한글 5개 + `후원 바로가기` CTA(구글폼 직결)
  - 높이 `80px`, 메뉴 그룹 오토레이아웃 `gap: 48px`, **완전 중앙 정렬**(시안 오차 무시)
  - CTA `144×37`, 배경 `ds-key2`, 라벨 `후원 바로가기`
  - 활성 메뉴 = **노랑 + 밑줄** — 새 시안에서 확정, #26 보류 해제
  - GNB 라벨은 `나지르에 대하여`(꺾쇠 없음), 페이지 제목은 `<나지르>에 대하여` — 표기 통일 여부 확인 필요
- [x] **푸터 교체** — 3컬럼(`문의` / `더 알아보기` / `후원하기`), 균등 3분할,
      간격 `64px`, 구분선 `0.5px solid #FFFFFF`
  - 연락처는 **마스킹 없이 시안 그대로 노출** — 대표 확인 완료 (코멘트 #47)
  - GNB 로고·파비콘은 심볼 이미지로 교체 완료(2026-09-10)
  - 푸터 워드마크는 **심볼 + 글자 잠금**으로 마감(2026-09-11). 가로형 로고 이미지는 여전히 없고,
    받은 자산이 정사각 심볼뿐이라 가로로 눕는 자리에 그대로 쓸 수 없어 나란히 묶었다.
    가로형 워드마크가 오면 그 블록만 `<img>` 하나로 바꾸면 된다 (코멘트 #24)
- [x] 콘텐츠 컨테이너 `max-w-content`(1398px) 적용

## 2단계 — 기존 페이지 시안 반영

각 페이지를 만들 때 `ds-*` 토큰을 씁니다. 옛 토큰은 마지막에 제거합니다.

| 시안 화면 | 라우트 | 상태 |
|---|---|---|
| 랜딩페이지 | `/` | ✅ 완료 |
| 〈나지르〉에 대하여 4개 | `/about/[slug]` | ✅ 완료 |
| 제작 과정 2개 | `/process/[slug]` | ✅ 완료 |
| 함께하는 사람들 3탭 | `/people?tab=` | ✅ 완료 |
| 후원과 기도 2개 | `/join/[slug]` | ✅ 완료 |
| 응원 게시판 | `/guestbook` | ✅ 완료 (3단계) |

상위 메뉴는 전부 `/[상위]/[slug]` 구조이고, 상위 경로는 첫 장으로 리다이렉트합니다.
하단 도트는 `components/SectionDots.tsx` 하나를 공유합니다.

- [x] **랜딩페이지** — 히어로(`1920×757`, 오버레이는 이미지에 구워져 있음) + 카드 5개
      (`gap: 12px`, 각 `270×350`, 반경 8px, 그림자)
  - 아이콘 원본이 `181×160`~`139×122`로 제각각 → 통일 박스 + `object-fit: contain`으로 흡수 (#3)
- [x] **\<나지르\>에 대하여** — 4개 서브페이지 `/about/[slug]`
      (01 `greeting` / 02 `praysound` / 03 `work` / 04 `characters`), `/about`은 01로 리다이렉트
  - 순서: 작품 개요 → 로그라인·시놉시스 → 주요 등장인물 (코멘트 #29) ✅
  - 편지 캐러셀 (01·02에만) — 어드민 관리형. 장수는 등록한 개수만큼 `1 / N`이고
    시안의 `1 / 5`는 고정값이 아니다(디자이너 확인 완료). `0008` 마이그레이션 필요
  - 페이지네이션 도트 **4개** — 시안 수정 완료 (코멘트 #45) ✅
  - 좌측 본문은 01·02만 채웠다. 03·04는 시안이 더미라 비워뒀다(우측과 중복 방지)
- [x] **제작 과정** — `/process/[slug]` 2개 (01 `schedule` / 02 `budget`)
  - 01 카드 2장 — `지나온 이야기`(완료) 어두운 카드 / `앞으로 걸어갈 이야기`(진행 중·예정) 노란 카드
  - 기본 5개 + `외 N개의 기록 더보기` (시안은 둘 다 "외 4개"로 복붙돼 있어 실제 남은 개수로 계산)
  - 02 총액 + 항목 8개 2열, 금액 비공개 (코멘트 #31)
  - 총액 표기는 관리자에서 `9,000,000 원` 으로 고쳤다(2026-09-11). 시안은 `9,000,000원`(붙임)이라
    공백 하나가 다르다 — 눈에 띄는 차이는 아니어서 그대로 둔다
- [x] **함께하는 사람들** — 최상위 라우트 신설, 탭 3개(헤더진/스탭진/배우)
  - 그리드 `1398×708`, 행 `gap: 8px` / 열 `gap: 12px`, 카드 `270×350`
  - 카드 = 사진 + 역할 + 이름, 클릭 시 `/people/[id]` (코멘트 #37)
  - ~~코멘트 #46 답변 전까지 카드 링크 구조는 확정 금지~~ → **개인 페이지로 이동 확정** (2026-09-11)
  - 사진은 기존 `PhotoField` + Supabase Storage 재사용
- [x] **후원과 기도** — `/join/[slug]` 2개 (01 `support` / 02 `prayer`), `/join`은 01로 리다이렉트
  - 01 후원 안내(구글폼 버튼 + 점선 계좌 박스 + 복사 버튼) · 02 기도 제목 6개 ✅
  - ~~기존 Q&A 블록(joey.team 외부 링크) 제거로 방문자가 메시지를 남길 경로가 없다~~ →
    **해소.** 응원 게시판이 열렸다

## 3단계 — 응원 게시판 (신규)

설계 트레이드오프는 `decisions.md` "응원 게시판" 절에 정리돼 있습니다.

**읽기·쓰기·답글·선별·하트까지 완료.**

- [x] 테이블 마이그레이션 `0009_guestbook.sql`
  - `guestbook_entries` — 이름, 메시지, 생성일, `ip_hash`, `is_held`(보류 플래그)
  - `is_hearted` · `guestbook_replies`는 확정 후 별도 마이그레이션
  - 공개 읽기는 RLS로 `is_held = false`만. anon 직접 insert는 막고
    `submit_guestbook_entry` SECURITY DEFINER 함수로만 받는다
    (브라우저에 노출된 anon 키로 우회 삽입할 수 없게 — `record_visit`과 같은 패턴)
- [x] 공개 작성 API `POST /api/guestbook` + 봇 대응 4종
  - Rate limit — IP 해시, **시간당 15건** (단체 관람 오탐 여유)
  - Honeypot 필드 (`autocomplete="off"`, 화면 밖 배치)
  - 제출 시간 검사 (3초 미만 차단)
  - 링크 포함 글 보류 (삭제 아님)
  - 허니팟·시간 위반은 이유를 알려주지 않는다(우회 힌트가 되므로)
- [x] 쪽지 카드 렌더
  - 악보 배경 정사각형(`note-square`)·직사각형(`note-wide`) 교대
  - 테이프 `77×36`, 각도 **8° / −8° 교대**
  - 배치: 좌상단 이름 / 가운데 내용 / 우하단 날짜
  - 좌하단 댓글 아이콘은 **답글이 있는 쪽지에만** 붙는다(눌러도 안 되는 아이콘 방지)
- [x] **페이지네이션** — `?page=N`, 한 페이지 24개
  - 시안·명세에 게시판 페이지네이션 UI가 없어 편지 캐러셀의 `‹ 1 / N ›` 표기를 가져왔다
  - 링크라서 공유·뒤로가기가 되고, 글을 남긴 뒤 서버가 다시 그려도 어긋날 상태가 없다
  - 범위 밖·잘못된 `page` 값은 404 대신 1페이지로 떨어진다
  - 글을 남기면 1페이지로 보낸다(새 글이 항상 맨 앞에 붙으므로)
- [x] **대댓글 확장 인터랙션** — 아이콘을 누르면 같은 종이가 늘어난다 (`0011`)
  - 에셋 추가 요청은 **필요 없었다.** CSS `border-image` 가 한 장을 9칸으로 잘라
    가운데만 반복하므로 3분할 이미지가 필요하다는 전제가 틀렸다 — `decisions.md` C-2
  - 자르는 값은 `lib/guestbook.ts` 의 `noteStyle`. 답글 4개에서 224px → 437px 로 늘어나며
    오선 간격과 찢어진 가장자리가 유지되는 것을 확인했다
- [x] **어드민 검토·삭제** — `/admin/guestbook`. 숨김 글이 위로 오고 공개/숨김/삭제.
      삭제는 2단계 확인
- [x] **어드민 답글 작성·수정·삭제** — `/admin/guestbook`
- [x] **욕설·광고·도배 규칙 선별** (`0012`) — `lib/moderation.ts`. AI 판정은 붙이지 않았다
      (스팸이 실제로 얼마나 오는지 보고 정하기로 함) — `decisions.md` C-5
- [x] **검토 대기 배지** — `/admin` 첫 화면. 보류 글이 생겨도 알림 경로가 없어
      운영진이 게시판에 들어가 보기 전까지 아무도 몰랐다
- [x] **제작팀 하트** (`0014`) — "응원 쪽지 하트는 제작팀용"으로 확정(2026-09-11).
      어드민 `하트 보내기/거두기`로 켜고 끄며, 쪽지 하단 왼쪽(댓글 아이콘 뒤)에 하트 + `제작팀`이 붙는다.
      방문자 좋아요는 없다 — `decisions.md` C-3
      ⚠️ **`0014` 적용 전에 배포하면 응원 게시판 전체가 안 뜬다.** 조회가 없는 컬럼을 부르면
      Supabase가 42703 에러를 돌려주기 때문이다. 반드시 SQL Editor에서 먼저 실행한 뒤 푸시한다
- [ ] 응원글 **내용 수정** — 지금은 숨김·삭제만 가능 (명세 46행)

## 4단계 — 정리

- [x] **옛 팔레트 제거** — `gold` / `gold-soft` / `gold-deep` / `paper` / `velvet` / `velvet-2` /
      `ink` / `stage` 를 전부 `ds-*` 로 옮기고 `tailwind.config.ts` 에서 삭제했습니다(203곳).
      공개 화면뿐 아니라 관리자 화면도 같은 토큰을 씁니다.
- [x] **Google Fonts `<link>` 제거** — 공개 화면은 자체 호스팅하는 시안 폰트만 쓰고,
      관리자 화면과 기본 본문은 시스템 폰트로 갑니다. 외부 폰트 요청 0건.
- [x] **미사용 컴포넌트 정리** — `HeroBackdrop` · `Spotlight` · `Accordion` · `StatusChip`.
      랜딩·제작 과정을 새 시안으로 갈아엎으면서 참조가 사라졌습니다.
- [ ] 본문 폰트가 확정되면(코멘트 #38) 해당 폰트만 `<link rel="preload">` 추가

## 5단계 — 모바일 (웹 확정 후)

웹 레이아웃이 굳기 전에는 손대지 않습니다. Figma 모바일 시안은 **랜딩 1장만** 있고 나머지 11개는
미착수입니다. 좌표·변환 규칙·주의사항은 [mobile-ui.md](mobile-ui.md)에 있습니다.

- [x] **모바일 브레이크포인트 구현** — 랜딩 시안에서 확정된 390 / 24 / 342 규칙 기준
  - GNB 높이 56px + 햄버거 (열면 메뉴 5개 + 후원 CTA, 항목을 누르면 닫힘)
  - 세부 페이지는 좌우 **스와이프**로도 넘어갑니다. 세로 스크롤을 가로채지 않도록
    가로 이동이 세로보다 1.5배 이상일 때만 반응합니다. 편지 캐러셀 위에서는
    페이지가 아니라 편지 장이 넘어갑니다
  - 랜딩 카드 가로형 1열 (아이콘 88×88 + 텍스트), 설명 불투명도 0.5 → 0.8
  - 좌우 여백 24px (`px-6 lg:px-8`), 푸터 1컬럼, 사람들 그리드 2열, 쪽지 1열
  - **데스크톱 전환 지점은 `xl`(1280)** 입니다. `lg`(1024)로 두면 아이패드 세로에서
    GNB 메뉴와 `후원 바로가기` CTA가 겹치고 랜딩 카드 5열의 글자가 넘칩니다
  - 히어로는 시안 크롭을 쓰지 않고 **포스터 전체 + 배경색 이어붙이기**로 갑니다.
    시안 크롭(1920×757)은 헤드라인 윗줄을 잘라먹는데, 글자가 포스터 세로의 70%를
    넘어서 2.5:1 띠에는 구조적으로 안 들어갑니다. 비율은 3:4 → 3:2 → 16:9 로 넓어집니다
  - 전 라우트에서 가로 넘침 0 확인 (375 폭 기준)
- [ ] Figma 모바일 시안 나머지 11개 — **Figma 데스크톱 앱에서 직접** (MCP는 시안 폰트를 못 씀).
      구현은 위 규칙으로 이미 반응형이라, 시안이 나오면 대조·보정만 하면 된다


## 6단계 — 운영 보강 (2026-09-11)

시안에는 없지만 실제로 운영하려면 있어야 했던 것들입니다.

- [x] **`og:url` · `canonical`** — 모든 페이지가 홈 주소를 가리키고 있었습니다. `lib/pageMeta.ts`
  - Next 는 `openGraph` 를 **얕게 합칩니다.** 자식에서 선언하면 부모의 `og:image` 가 통째로 사라져,
    공유 카드에서 그림만 빠집니다. 그래서 `OG_BASE` 를 펼쳐 넣습니다
- [x] **개인 상세 `/people/[id]`** — 나머지 화면과 같은 규칙으로 다시 짰습니다
- [x] **욕설·광고·도배 규칙 선별** — `lib/moderation.ts` (`decisions.md` C-5)
  - 정규화가 **두 벌**입니다. 하나는 숫자를 살리고(전화번호), 하나는 숫자를 글자로 바꿉니다
    (`0→o`, `1→l` 같은 우회). 한 벌로 합치면 `010-1234-5678` 이 `olol234s678` 이 되어
    전화번호가 영영 안 걸립니다
- [x] **어드민 목록 도구** — 찾기 · 거르기 · 모아 보기 · 쪽 나눔 (`lib/adminView.ts`)
  - 열 개를 넘거나 거를 기준이 있을 때만 나타납니다. 짧은 목록에서는 자리만 차지합니다
  - 걸러낸 행도 **저장에는 그대로 들어갑니다.** 화면에 그렇게 적어 두었습니다
- [x] **어드민 실시간 미리보기** — 편집 중인 값으로 공개 화면의 그 카드를 그대로 그립니다
  - 미리보기용 사본을 만들지 않고 공개 페이지가 쓰는 컴포넌트를 그대로 부릅니다.
    사본을 두면 언젠가 둘이 갈라지고, 그때부터 미리보기는 거짓말을 합니다
  - 미리보기가 터져도 편집기는 살아 있어야 하므로 경계를 뒀습니다
- [x] **문구 편집 화면 "나오는 곳" 링크** — 칸 이름만 보고는 어느 페이지 어느 자리인지 알 수 없었습니다
- [x] **모바일 스와이프 온보딩** — 처음 방문한 사람은 밀어서 넘기는 것을 모릅니다
- [x] **버튼 화살표를 SVG 로** — `‹` `›` 는 시안 폰트에 **빈 글리프**로 들어 있습니다.
      코드포인트가 매핑돼 있으니 브라우저가 폴백을 쓰지 않고, 0 픽셀을 그립니다

---

## 답변 대기 중인 것

| 항목 | 누구에게 | 영향 범위 | 웹 1·2단계 차단? |
|---|---|---|---|
| **랜딩 카드 아이콘 크기 통일** (이미지는 `icon1~5.png`로 교체됨, 크기는 제각각 그대로) | 홍빛 | 랜딩페이지 | 아니오 — `object-fit`으로 흡수 |
| **가로형 워드마크** (푸터는 심볼 + 글자 잠금으로 마감) | 홍빛 | 푸터 | 아니오 |
| **히어로 이미지 최종본 여부** (`1.png`로 교체됨) | 홍빛 | 랜딩페이지 | 아니오 |

**해소된 항목**

- ~~"우리 하트"의 의미~~ → **제작팀 하트로 확정** (2026-09-11). `is_hearted` boolean 하나, 방문자 좋아요 없음
- ~~함께하는 사람들 카드 클릭 → 상세페이지 여부~~ → **개인 페이지로 이동 확정** (2026-09-11). `/people/[id]` 유지
- ~~본문 폰트~~ → **제목·본문은 빛의계승자, 제목 아래 소개 문단만 Apple SD Gothic Neo UltraLight** (2026-09-11).
  애플 외 기기는 Pretendard ExtraLight로 대체 (`font-desc`)
- ~~GNB 활성 밑줄 간격~~ → 새 시안에서 노랑 + 밑줄로 확정 (#26)
- ~~푸터 전화번호 공개 여부~~ → **마스킹 없이 공개로 확정** (#47, 대표 확인 완료)
- ~~대하여 페이지네이션 도트 개수~~ → 4개로 시안 수정 완료 (#45)
- ~~대댓글 확장용 이미지 에셋~~ → **필요 없었다.** `border-image` 로 한 장을 9칸으로 잘라 씀 (C-2)

**지금 화면을 막고 있는 것은 없습니다.** 남은 3건은 전부 디자이너 최종 에셋이라, 오면 파일만 교체합니다.

---

## 배포 전 점검

- [x] **폰트 woff2 변환** — Heir of Light Regular/Bold를 OTF에서 woff2로 만들었습니다.
      `795KB → 451KB`, `809KB → 457KB`. globals.css가 woff2를 먼저 찾으므로
      **매 방문 나던 404 2건이 사라졌고** OTF는 폴백으로만 남습니다.
      (변환은 `wawoff2`를 임시 설치해 1회 수행 후 제거했습니다. package.json 변화 없음)
- [x] **캐시 헤더 확인** — `/fonts/*` 에 `public, max-age=31536000, immutable` 적용 확인
- [x] **프로덕션 빌드 확인** — `next build` 통과. 정적/동적 분리도 의도대로입니다
      (콘텐츠 페이지 SSG + 1분 revalidate, `/guestbook`·`/admin/*`·API는 동적)
- [x] **`NEXT_PUBLIC_SITE_URL` 설정** — `https://nazir.cloud` 적용 완료(2026-09-11).
      비면 Vercel 자동 도메인(`*.vercel.app`)을 쓰는데, 카톡 공유 썸네일과 sitemap.xml 이 그 주소로 나갑니다.
      Production·Preview·Development 세 환경 모두에 걸어두면 프리뷰 배포에서도 같은 주소를 씁니다
- [ ] Vercel 배포 후 `/fonts/HeirOfLight-Regular.woff2` 200 확인
- [ ] 배포 후 공유 카드 확인 — 카카오톡 채팅에 링크를 붙여넣어 썸네일·제목이 뜨는지
- [x] **마이그레이션 적용** — `0007` ~ `0013` 전부 적용 완료.
- [x] **`0014_guestbook_heart.sql` 적용** — 2026-09-11 적용 후 push. 미적용 상태로 배포하면 응원 게시판 전체가 안 뜬다
      Supabase 대시보드 SQL Editor 에서 직접 실행합니다
  - `0010` 편지 2장 · `0011` 답글 · `0012` 보류 사유 · `0013` Praysound 편지
- [x] **관리자에서 고칠 값** — 은행명 `카카오뱅크`, 예산 총액 `9,000,000 원` 으로 정정 완료(2026-09-11)
- [x] **`VISIT_IP_SALT` 설정** — Secret 으로 등록 완료(2026-09-11). 비면 코드의 기본값
      (`'nazir-visit'`, 저장소에 공개돼 있음)을 써서 방문자 IP 해시를 역산할 수 있었습니다
  - [ ] 소금을 넣기 전에 쌓인 `visits.ip_hash` 는 옛 기본값으로 해시돼 있어 아직 역산 가능합니다.
        하루 상한을 세는 용도라 어제 이전 것은 지워도 집계에 영향이 없습니다
- [ ] Vercel Hobby는 약관상 **개인·비상업 프로젝트 전용**.
      후원 계좌를 안내하는 사이트라 완전히 무관하다고 보기는 어려움 — 인지 필요
- [ ] **GriunGossi 2.7MB** — 응원 게시판에서만 쓰지만 그 페이지 첫 방문에 2.7MB를 받습니다.
      쪽지 내용이 방문자가 쓰는 한글이라 서브셋이 위험해 손대지 않았습니다.
      부담되면 ① 쪽지 폰트를 시스템 폰트로 바꾸거나 ② 자주 쓰는 음절만 서브셋 + 나머지는
      폴백으로 두는 방법이 있습니다 — 결정 필요
- [ ] 폰트 전송량(현재): 첫 방문 **약 0.9MB**(Heir 2종), 응원 게시판 진입 시 **+2.7MB**(Griun).
      재방문은 캐시로 0. Hobby 100GB/월 기준 단순 계산 약 110,000 신규 방문분

---

## 다른 기기에서 이어서 작업할 때

```bash
git pull
npm install
npm run dev
```

폰트 파일은 저장소에 포함돼 있어 별도 내려받기가 필요 없습니다.
`.claude/`는 gitignore 대상이라 `launch.json`은 따라오지 않습니다.
